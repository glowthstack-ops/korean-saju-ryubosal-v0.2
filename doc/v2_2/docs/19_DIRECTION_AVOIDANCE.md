# 19. 피할 방향 판정 + 민속 흉방 레이어 (Direction Avoidance & Folk Taboo)

> **상태: 2026-09-21 데굴님 승인·구현 완료(D1~D4).** 결정(§9): a=합집합, b=세운 역할·도메인 등급 매핑,
> c=여행 포함(이사·이동 테마 전체 블록), d=택일 근거 줄만, e=신규 목적 3종만('비공개 협상' 미도입),
> f=세운 카드 배지 포함. 민속 흉방은 "민속에서는 ○쪽은 ○○한 이유로 피하는 방향" 형태의 **추가 정보**로만
> 제공하고, 테마사주(RPT_FOCUS 행동 전략 섹션)·AI 채팅상담에도 같은 판정·고지를 싣는다(데굴님 지시).
> 원천: 2026-09-21 데굴님 제공 자료 2편("피해야 할 방향", "무속에서의 흉방") + docs/18(12신살 방위 활용 계층).
> docs/18의 계산 규칙·사전 스키마·13목적은 그대로 유지하고, 본 문서는 그 위에 **회피 판정**과
> **민속 흉방 레이어**를 얹는다. 목록·표·등급은 규격이며 임의 추가·삭제·재해석을 금지한다(CLAUDE.md 원칙 10).

## 0. 원칙 (자료 §1·§6·§7·§9 요약)

1. **개인 12신살 층에는 절대흉방이 없다** (`ABSOLUTE_AVOID = 없음`). 겁살·재살·천살·삼재 방향이라서 무조건 피하는
   규칙을 만들지 않는다. 같은 방향도 목적에 따라 +/−가 바뀐다(재살: 화장대엔 부적합·공부엔 활용).
2. **회피는 3단계** — ① 목적상 부적합(CAUTION) → ② 동일 불리 신호가 시간·공간에서 중첩(STRONG_AVOID).
   "피함"은 흉사 예고가 아니라 **그 목적과 성질이 맞지 않는다**는 뜻이다.
3. **민속 흉방은 별도 레이어**(FOLK_TABOO). 개인 사주·목적과 무관하게 그해·그날 공통으로 적용되는 방위 금기이며,
   개인 12신살 방향을 지우지 않고 **동시에 존재**한다("북쪽은 년살(매력)이지만 올해 삼살방이라 이사·증축은 별도 주의").
4. **기원 분리** — 삼살방(그해 지지 기준)과 12신살 겁·재·천(개인 연지 기준)은 표가 같아 보여도 코드에서 같은 객체로
   합치지 않는다. 삼재(개인 액년) ≠ 삼살방(연간 공통 흉방).
5. **지지 단위** — "서쪽은 흉하다"가 아니라 "서쪽 중 酉 재살은 소개팅에 부적합, 戌 천살은 공부에 활용" 수준으로 내려간다.
   대방위(동/남/서/북)와 세부방위(12지지)를 모두 보존한다(docs/18 §1-4와 동일).
6. **판정은 엔진, LLM은 재서술**(절대원칙 1). 등급·중첩 조건·민속 흉방 모두 코드가 계산해 블록으로 준다.
7. **서술 전용(inert)** — 사건 점수·날짜·간지 파이프라인 불변. 택일 모듈에는 **필터/근거 줄**로만 들어간다(점수 가중 아님).

## 1. 판정 등급 5단계 (자료 §9)

| 등급 | 한글 라벨 | 원천 | 의미 |
|---|---|---|---|
| `BEST_USE` | 적극 활용 | 사전 grade `fit` | 해당 목적에 적극 활용 |
| `GOOD_USE` | 잘 맞음 | 사전 grade `support` | 비교적 잘 맞음 |
| `NEUTRAL` | 중립 | 사전 grade `neutral` | 특별한 유불리 없음 |
| `CAUTION` | 주의(목적 충돌) | 사전 grade `caution` | 목적과 성질이 충돌 |
| `STRONG_AVOID` | 강한 회피 | **계산**(§3) | 동일 불리 신호가 시간·공간에서 중첩 |

- 사전(`sinsal_direction.json`)의 4등급 문자열은 바꾸지 않는다. 5등급은 `DirectionPick.verdict`(신규 필드)로 계산해 얹는다.
- `ABSOLUTE_AVOID`는 만들지 않는다. `forbidden_framings`("○쪽은 나쁜 방향이다"·"흉방이니 피하라")는 유지한다.
- 허용 문형(추가): **목적 한정 회피** — "잠자리 머리 방향으로는 동쪽 寅 역마살 쪽을 피하는 편이 좋습니다".
  목적 없이 방향 자체를 흉하다고 쓰는 문장은 계속 금지.

## 2. 12신살별 회피·활용 맥락 (자료 §1) — 사전 `sinsals[]` 필드 추가

`SinsalDirectionEntry`에 `avoid_contexts: list[str]`·`use_contexts: list[str]` 를 추가하고 아래를 시드한다
(기존 `cautions`/`action_hints`는 유지 — 이 필드는 "어떤 상황에서 −/+가 되는가"의 근거 목록).

| 신살 | 기본 작용 | 특히 피하는 상황(avoid_contexts) | 활용 가능 상황(use_contexts) |
|---|---|---|---|
| 겁살 | 경쟁·쟁취·긴장 | 화해, 협업 합의, 편안한 데이트, 공동분배 | 경쟁, 승부, 집중 |
| 재살 | 구속·얽힘·지연 | 빠른 의사결정, 데이트, 즉흥적 표현, 빠른 회전 업무 | 장시간 공부, 검토 |
| 천살 | 외압·정지·통제 어려움 | 영업, 빠른 실행, 적극적 확장 | 공부, 연구, 사색, 스승 |
| 지살 | 이동·출입·활동 | 숙면, 장시간 정적인 작업 | 영업, 외근, 이동, 네트워킹 |
| 년살 | 매력·노출·도화 | 비밀 유지, 은둔 작업, 노출을 피해야 하는 일 | 소개팅, 화장, 촬영, 브랜딩 |
| 월살 | 고초·성장통·과정 부담 | 소개팅, 중요한 첫인상, 편안함이 중요한 자리 | 제작 과정, 반복 작업 |
| 망신살 | 드러남·노출·화제 | 비밀 상담, 기밀 협상, 숨겨야 할 일 | 발표, 콘텐츠 공개, 홍보 |
| 장성살 | 권한·주도·강한 자기 주장 | 화해, 양보가 필요한 협상, 상대 중심 상담 | 리더십, 지휘, 결정 |
| 반안살 | 안착·정착·안정 | 급격한 변화, 빠른 이동이 핵심인 활동 | 숙면, 휴식, 정착 |
| 역마살 | 이동·변동·환경 변화 | 숙면, 정착, 차분한 장기 집중 | 여행, 이사, 변화, 외부활동 |
| 육해살 | 마찰·적응 부담·얽힘 | 민감한 관계 협상, 소개팅, 갈등 해결의 첫 자리 | 변화 후 적응·문제 파악 |
| 화개살 | 수렴·고독·정신성 | 영업, 사교, 대중 노출, 활발한 네트워킹 | 상담, 명상, 연구, 자기성찰 |

## 3. 목적별 피하는 방향 (자료 §2) — 기존 13목적과의 통합

자료 표를 기존 사전 `grades`와 대조한 결과. **통합 방식은 결정 필요(§9-a)** — 아래는 합집합(union) 기준 제안.

| 목적 | 기존 caution | 자료 '피하는 방향' | 제안(합집합) | 비고 |
|---|---|---|---|---|
| 소개팅·데이트 | 재·월·화개 | 재·월·육해 | 재·월·육해·화개 | 육해 neutral→caution |
| 메이크업·외모 | 재·월 | 재·월 | 재·월 | 동일 |
| 영업·장사 | 겁·재·화개 | 천·화개 | 겁·재·천·화개 | 천살 neutral→caution |
| SNS·브랜딩 | 재·월·화개 | 화개 | 재·월·화개 | 기존 유지 |
| 발표·인지도 | 천·화개 | 재·화개 | 천·재·화개 | 재살 caution 추가; 자료 우선활용 '장성'은 기존 support 유지 |
| 리더십·협상 | 겁·천·육해 | 겁·육해(협상·계약) | 겁·천·육해 | 기존 유지 |
| 공부·시험 | 년·역마 | 지·역마 | 년·지·역마 | 지살 neutral→caution |
| 연구·기획 | 년·역마 | 지·년(연구·집필) | 지·년·역마 | 지살 neutral→caution |
| 숙면 | 지·년·역마 | 역마·지 | 지·년·역마 | 기존 유지 |
| 여행·이동 | 재·육해 | 반안 | 재·육해·반안 | 반안 neutral→caution |
| 이사·환경 변화 | 겁·재·육해 | (자료 없음) | 겁·재·육해 | 기존 유지 |
| 상담·명상 | 지·년·망신 | 년·망신 | 지·년·망신 | 기존 유지 |
| 자기성찰·정리 | 지·년 | (연구·집필 준용) | 지·년 | 기존 유지 |

**신규 목적 3종(13→16)** — 자료 §2에만 있는 목적. 12신살 전체 등급은 우선 활용=fit, 피함=caution, 나머지=neutral로
시드하고, 파서 keywords는 §9-e에서 확정한다.

| purpose | name_ko | usage_mode | fit | support | caution | domain | trigger_domains |
|---|---|---|---|---|---|---|---|
| `reconciliation` | 화해·관계회복 | position | 반안살 | — | 겁살·장성살·육해살 | relationship | [] |
| `new_start` | 새로운 일 시작 | position | 지살·역마살 | — | 재살 | career | [] |
| `settling` | 안정·정착 | position | 반안살 | — | 역마살 | relocation | [] |

- 자료의 '협상·계약'은 기존 `leadership`(리더십·협상)에, '연구·집필'은 `research`에, '발표·무대'는 `presentation`에 흡수한다(목적 중복 금지).
- lint 규칙(docs/18 §3)은 그대로: 12신살 전수·목적 1순위=fit·primary에 caution 금지.

## 4. STRONG_AVOID — 조건 중첩 판정 (자료 §3·§4·§5·§7)

"흉한 방향 하나가 있어서 피하는 게 아니라, **동일한 부정적 주제가 시간과 공간에서 중첩될 때** 강한 회피."

### 4-1. 조건 4개 (기준 연도 = 오늘의 입춘 연도, 질문 시점이 있으면 그 연도)

| # | 조건 | 판정 신호(기존 엔진 재사용 — 신규 명리 규칙 없음) |
|---|---|---|
| ① | 목적과 방향 신살의 성향이 강하게 반대 | 사전 grade == `caution` |
| ② | 현재 세운의 12신살과 방향 신살이 중첩 | `relative_twelve_sinsal(연지, 세운 지지) == 방향 신살` ⇔ 방향 지지 == 세운 지지. 삼재 해에는 자동으로 들=역마/눌=육해/날=화개 테마 중첩이 된다(자료 §4의 세 조합을 포함) |
| ③ | 악삼재 또는 불리한 세운 | 삼재 해: `SamjaeInfo.quality == ak`. 비삼재 해: 세운 기둥 역할이 기신·구신(기존 용희기구한 판정) — **§9-b 승인 필요** |
| ④ | 원국·대운에서 동일 영역의 부정 신호 활성 | 목적 `domain`에 대한 `SamjaeDomainGrade.grade == caution`(사건 후보 net 방향, `samjae_quality`의 도메인 합성 함수를 비삼재 해에도 쓸 수 있게 분리) — **§9-b 승인 필요** |

- **판정**: ①이 참이고 ②③④ 중 **2개 이상**(= 4조건 중 3개 이상) → `STRONG_AVOID`. ①만 → `CAUTION`. ①이 거짓이면 회피 없음.
- **반대 방향(GOOD_MATCH)**: grade `fit`/`support` + ② 참 + 복삼재(또는 세운 용신·희신) → `BEST_USE`에 "시간·공간 일치" 근거 줄을 붙인다. 등급을 새로 만들지 않는다(자료 §5 "삼재 자체를 감점하지 않고 목적 일치 여부를 판정").
- **출력**: `DirectionPick.verdict` + `verdict_evidence: list[str]`(한글 근거 — "현재 들삼재(역마살 세운)와 방향 신살이 겹침" 등). 수치 미노출.
- 자료의 사례 A(숙면·역마·들삼재·악삼재 → 강한 회피), B(소개팅·육해·눌삼재·악삼재·관계 불리 → 강한 회피),
  C(비공개 협상·망신·세운 망신 활성·원국 노출 → 강한 회피)를 회귀 픽스처로 고정한다. C는 목적 `leadership`(협상)에 망신살
  caution이 없으므로 **'비공개 협상'을 별도 목적으로 둘지 §9-e에서 결정**.

### 4-2. 문구(자료 사례 A)

> 현재는 이동과 변화의 신호가 강하게 겹치는 방향입니다. 안정과 숙면이 목적이라면 다른 방향을 우선 고려하는 편이 좋습니다.

## 5. 민속 흉방 레이어 (자료 후반부) — `FOLK_TABOO`

### 5-1. 종류·계산 (기준: 연간 = 만세력 年支(입춘), 일간 = 음력 일 — `solar_to_lunar` 재사용)

| 흉방 | 기준 | 계산표 | 성격 | 전통적으로 꺼리는 행위 | 중요도 |
|---|---|---|---|---|---|
| 삼살방(三煞方) | 그해 연지 삼합 | 寅午戌년→북(亥子丑) / 巳酉丑년→동(寅卯辰) / 申子辰년→남(巳午未) / 亥卯未년→서(申酉戌) | 해마다 이동 | 이사, 개업, 증축, 터파기, 큰 공사 | 매우 높음 |
| 대장군방(大將軍方) | 그해 연지 방합 | 亥子丑년→서 / 寅卯辰년→북 / 巳午未년→동 / 申酉戌년→남 | 3년 고정 | 이사, 건축, 수리, 증축, 토석공사, 이장 | 매우 높음 |
| 태세방(太歲方) | 그해 연지 | 연지의 지지 방향(子북·午남…; 12지지 세부) | 그해 태세 자리 | 큰 공사, 땅을 건드리는 행위 | 높음 |
| 세파방(歲破方) | 태세의 충 | 연지 충 지지의 방향 | 태세 정반대 | 이사·이동·큰 변동 | 높음 |
| 손방(損方) | 음력 일 끝자리 | 1·2→동 / 3·4→남 / 5·6→서 / 7·8→북 / 9·0→없음(손 없는 날) | 날짜마다 이동 | 이사·개업·혼례 등 이동 | 높음 |
| 세형·황번·표미·태음 등(팔장신) | — | — | 행위별 특수 | 경작·토목·혼인·출산 | **범위 제외(선택)** |
| 오황살·암검살(구성) | — | — | 연·월 공간 | 공사·장기 체류 | **범위 제외(별도 체계)** |
| 절명·오귀·육살·화해(팔택) | 개인 괘명 | — | 개인 고정 | 주거·침실·문·책상 | **범위 제외(개인화 확장)** |

- 2026 병오년 검산: 삼살=북, 대장군=동(巳午未 3년), 태세=남(午), 세파=북(子) → **북 = 삼살+세파 중첩**, 동 = 대장군.
- 기존 `calendar/son_eomneun_nal.json`(손 없는 날 = 음력 9·0일)은 유지하고, 손방은 **같은 규칙의 방향 확장**으로
  새 사전에 둔다(택일 모듈의 "손없는 날" 가점은 불변).

### 5-2. 등급·적용 범위

| 등급 | 조건 |
|---|---|
| `FOLK_TABOO` | 그 방향에 연간 흉방 1종(또는 손방 1종) 해당 |
| `STRONG_FOLK_TABOO` | 같은 방향에 연간 흉방 2종 이상 중첩(삼살+대장군, 삼살+세파 등) |

- **적용 행위(appliesTo)**: 이사·개업·증축·터파기·큰 공사·인테리어·이장·혼례(손방). 즉 docs/18의 `usage_mode == move`
  목적(여행·이사)과 **공간을 크게 움직이는 행위**에만 본문 판정으로 들어간다.
- **비적용**: 바라보기·머리·위치·출입구 목적(공부·숙면·화장·촬영 등)에는 판정하지 않는다. "화장하는 것까지 삼살 때문에
  금지"는 전통 적용 범위의 과도한 확장이다. 단 방향 질문 답에는 **고지 한 줄**("올해 북쪽은 삼살방·세파방 — 이사·증축 등
  큰 공간 변동은 별도 주의")만 붙인다.
- **개인 층과의 관계**: 덮어쓰지 않는다. 블록에 `personal`(12신살 verdict)과 `folk`(흉방)를 **별도 줄**로 병기한다.
- **사전**: `dictionaries/folk_taboo_direction.json`(스키마 `shared_types/folk_direction.py`) — 표 5종·appliesTo·문구·
  `reviewed`. 12신살 사전과 파일·모듈을 분리한다(§0-4 기원 분리). validate → snapshot → drift 테스트 파이프라인 동일.
- **모듈**: `saju_engines/folk_direction.py` — `annual_taboos(year) -> list[FolkTaboo]`, `daily_son_direction(date)`,
  `folk_taboo_for(direction, year, day, action)`.

### 5-3. 소비처

| 소비처 | 내용 |
|---|---|
| 채팅 — 이사·여행·공사 질문 | `[민속 흉방 — 연간·일간]` 블록: 그해 4종(방향별 중첩 표시) + 질문 날짜의 손방. 기존 이사 방위(오행 길방·region_direction)와 **별도 줄**("오행 보완 방향 / 활용 방향 / 민속 흉방" 3분리) |
| 채팅 — 그 외 방향 질문 | 고지 한 줄만(5-2) |
| 택일(`date_selection` relocation) | 후보 날짜별 **손방** + 그해 연간 흉방을 근거 줄에 추가. 점수 가중 없음(필터/근거) — **§9-d 결정** |
| 리포트 F-20b·Y-11b | "올해의 민속 흉방" 한 줄(연간 4종·중첩) |
| 만세력 세운 카드 | 연간 흉방 4종 배지(삼재 배지 옆) |

## 6. 답 구성 (채팅 수동 방향 질문 — 되물음 없음, 2026-09-21 데굴님 결정)

1. **기본 방향**: 질문 목적의 BEST_USE(+GOOD_USE) — 사용 방식·지지·신살·행동 문장. 목적을 못 잡아도 사용 방식으로
   확정(머리→숙면, 바라보기→공부, 출입구→영업, 이동→여행)하고, 둘 다 없으면 되묻지 않고 2·3만으로 답한다.
2. **상황별 조언**: 사전 `purposes` **전체**(16종) 한 줄 표 — `목적(사용 방식): 적극 활용 → 잘 맞음 → 피함`.
   LLM은 사용자 상황(스레드 도메인·2단계 프로필)에 가까운 3~4개만 골라 **각 목적의 사용 방식으로** 짧게 서술한다.
   사용 방식 혼용 금지(docs/18 지침 ②)는 유지 — "공부가 중요하면 머리를 천살 쪽으로" 금지.
3. **피할 방향**: 질문 목적의 CAUTION 전부 + STRONG_AVOID(근거 줄 포함). 현재 구현의 "적합 방향과 같은 4방 안의 주의만"
   제한을 없앤다. 문구는 목적 한정(§1).
4. **민속 흉방 고지**: §5-2(이동·공사 목적이면 본문, 그 외 한 줄).
5. 승계 도메인 기반 **능동 목적은 방향 질문에서 쓰지 않는다**(1번 대화 오류의 직접 원인).
6. **프롬프트 구성(2026-09-21 승인)**: 방향 질문(Q10 REMEDY + `direction_question`)은 구조 질문(CHART_ANALYSIS)과 같이
   시점·이벤트 재료(사건 후보·월별 요약·유력 달·상담 결론/서술 계약·답변 지평·근거 경로·사건 서술 계약)와 도메인 보조(토픽
   모듈 신호·건강 취약 구조)를 싣지 않는다 — 실측 20.6k 중 약 8.8k(43%)가 방향 답과 무관했고 월별 서술로 새는 원인이었다. 원국 배경
   (명식 해석 자료·작동 역할·구조 패턴)과 공통 계약은 유지. 대화형 입력 상한은 22k→28k(docs/09 §8). 답 구성 계약
   `DIRECTION_ANSWER_DIRECTIVE`(기본 방향·상황별 3~4목적·피할 방향·민속 고지 4부분 필수). 이사·여행 방향은 기존 경로.

## 7. 파서·대화 계층 수정 (2026-09-21 결함 분석)

| # | 결함 | 수정 |
|---|---|---|
| P0-a | "잘때는"(붙여쓰기)이 머리 방향 어휘("잘 때"·"잠잘")·숙면 키워드에 없음 → 목적 미검출 | 머리 어휘에 `잘때·잘땐·취침·잠자리·자는·누워`, 숙면 keywords에 `잘때·잘 때·취침·잠자리` 추가 |
| P0-b | 직전 답이 되물음으로 끝나 `last_offer`가 있으면 12자 이하 방향 질문이 offer-slot(TIME_SHIFT)로 링크돼 query_type·domain 승계 + 되물음 답변 지시문 부착 | 방향 표지(`_DIRECTION_ASK_RE`) 또는 `direction_purpose`가 있는 발화는 offer-slot·offer-answer 링크와 `_OFFER_ANSWER_DIRECTIVE`에서 제외(시점 미승계 가드와 같은 자리) |
| P0-c | 목적 미검출 + 승계 education → 능동 트리거가 '공부(천살)' 블록을 수면 질문에 주입 | §6-5 — 방향 질문은 능동 목적 미사용, §6-1 기본값 규칙 |
| P2 | "책상"이 위치(position) 어휘에 있어 공부 목적의 사용 방식(face)을 덮음 | position 어휘에서 `책상` 제거(공부 키워드로만) |

## 8. 구현 단계 (각 단계 = 독립 PR, 완료 기준 = 타입 + 구현 + 단위 테스트 + 회귀 픽스처)

| 단계 | 내용 | 사전 버전 |
|---|---|---|
| D1 | §7 파서·링크 수정, §1 5등급 verdict 필드, §6 답 구성(피할 방향 전부·16종 표·지침), docs/18 §5-1 갱신 | 1.1.0 유지(코드만) |
| D2 | §4 STRONG_AVOID 중첩 판정(세운 12신살·삼재 quality·도메인 등급 재사용) + 사례 A/B/C 픽스처 | — |
| D3 | §5 민속 흉방 레이어(사전·스키마·모듈·스냅샷) + 채팅·리포트·세운 카드·택일 배선 | folk 1.0.0 |
| D4 | §2 신살 맥락 필드, §3 등급 통합 + 신규 목적 3종 + 파서 keywords, 스냅샷 재빌드 | 1.2.0 |

## 9. 결정 필요 (데굴님)

- **a. 등급 통합 방식**: §3 합집합(자료 피함을 기존 caution에 추가) vs 자료 우선(기존 caution 중 자료에 없는 것을 neutral로 완화). 제안 = 합집합.
- **b. 조건 ③④ 신호 매핑**: 비삼재 해의 "불리한 세운" = 세운 역할 기신·구신, "동일 영역 부정 신호" = 목적 도메인의 사건 후보 net caution. 다른 기준(위험 엔진 family 등)을 원하시면 지정.
- **c. 민속 흉방 적용 행위**: §5-2 목록(이사·개업·증축·터파기·큰 공사·인테리어·이장·혼례) 확정. 여행은 포함/제외?
- **d. 택일 반영 방식**: 손방·연간 흉방을 후보 날짜의 **근거 줄**로만(점수 불변) vs 회피일 필터처럼 후보 제외. 제안 = 근거 줄.
- **e. 신규 목적**: 화해·관계회복 / 새로운 일 시작 / 안정·정착 3종의 파서 keywords, 그리고 자료 사례 C의 '비공개 협상'을
  별도 목적(`private_negotiation`: fit 화개·천살, caution 망신·년살)으로 둘지.
- **f. 만세력 세운 카드 배지**: 연간 흉방 4종을 세운 카드에 표시할지(프론트 변경 동반).

## 9-1. 구현 위치 (2026-09-21)

| 구성 | 파일 |
|---|---|
| 5등급 verdict·근거 필드 | `shared_types/sinsal_direction.py` (`Verdict`, `DirectionPick.verdict/verdict_evidence/same_quadrant_as_fit`, `SinsalDirectionBlock.avoidance_basis`) |
| 중첩 판정 | `saju_engines/direction_avoidance.py` (`build_avoidance_context`·`decide_verdict`·`annotate_avoidance`·`strong_avoid_lines`), 도메인 등급 공용 `samjae_quality.domain_grades_for_year` |
| 민속 흉방 | `dictionaries/folk_taboo_direction.json` → `compiled/folk_taboo_direction_v1.0.0.json`(`scripts/build_folk_taboo_snapshot.py`), `shared_types/folk_direction.py`, `saju_engines/folk_direction.py`(연간 4종·손방·문구·`enrich_folk_taboos`), lint `_lint_folk_taboo` |
| 채팅 | `context_reducer`: `_direction_purposes`(방향 질문은 능동 미사용)·`annotate_avoidance`·`_folk_taboo_lines`(이사·이동·공사=전체, 그 외 방향 질문=고지 한 줄)·`LlmInput.folk_taboo_context`·`FOLK_TABOO_INSTRUCTION`; `chat_service` 되물음 지시문 가드 |
| 파서·대화 | `query_parser.is_direction_question`·`IntentJson.direction_question`·머리 어휘/표지 확장·'책상' 위치 어휘 제거; `conversation.link_question` offer 링크 제외·도메인 미승계 |
| 리포트 | `report_service`: F-20b·Y-11b(목적 전체 표+피함 열·강한 회피·민속 전체 블록), 테마 행동 전략 섹션(W-08·J-07·R-07·RL-07·C-07: verdict + 민속 전체/고지) |
| 택일 | `DateCandidate.folk_direction_note`(이사·개업·혼례 목적, `folk_note_for_day`) → 채팅 택일 표 notes |
| 만세력 | `LuckPillar.folk_taboos`(세운 전용, `manse_service`에서 `enrich_folk_taboos`), 프론트 세운 카드 '흉방' 배지(`Panels.tsx`, `types.ts FolkTabooHit`) |
| 사전 | `sinsal_direction.json` 1.2.0: 목적 16종·등급 합집합·`avoid_contexts/use_contexts`·숙면 keywords·forbidden_framings 추가 |

## 10. 회귀

- 파서: "잘때는 어떤방향이 좋을까"→(sleep, head); "공부할때 책상방향"→(study, face); "잘때 머리 방향"·"취침 방향" 등.
- 대화: 공부 방향 → (last_offer 있음) "잘때는 어떤방향이 좋을까" → 목적 sleep, link NEW, 되물음 지시문 없음, 블록에 '공부' 능동 목적 없음.
- 블록: 숙면 질문에 CAUTION 3종(지·년·역마) 전부 + 16종 표 + 민속 고지 한 줄. 申子辰 연지 → 丑 반안살 북쪽 유지(2번 대화 답 보존).
- STRONG_AVOID: 사례 A/B/C 픽스처(중첩 2/3/4조건 경계값 포함), 복삼재+역마+여행 → BEST_USE 근거 줄.
- 민속: 2026(午)=삼살 북·대장군 동·태세 남·세파 북(북 STRONG); 2025·2027 대장군 동 고정; 손방 음력 1~30 전수; 손 없는 날 기존 가점 불변.
- 스냅샷 드리프트·lint(12신살 전수·1순위 fit·primary caution 금지)·forbidden_framings 유지.
